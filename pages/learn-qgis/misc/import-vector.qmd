---
title: "Part 1 – Vector Import & Basemap"
description: "Import land use and neighbourhood boundary layers into QGIS, style them, and produce a basemap."
categories:
 - Data Preparation
level: Beginner
level_code: 1.0
level_color: '#A6CEE3'
date: 2026-08-06
---

**Classification:** Beginner GIS Analysis \| Spatial Data Basics \| Vector Import

**Learning Level:** Beginner

**Time Estimate:** 30 minutes

**Software Required:** QGIS LTR 3.44, QuickMapServices (QMS) plugin

**Author:** Daniele Cannatella

::: {.callout-important title="Note for TA"}
This is Part 1 of the MSc1 Urbanism Week 1 workshop (see `pages/courses/mscu_q1.qmd`, 10:00–10:30 slot). Scope is deliberately limited to import + styling + basemap to fit the 30-minute block

Remaining placeholders (`[...]`) to fill in before this goes out to students:

- Per the course plan, BBG + CBS neighbourhoods are already clipped to the study area and provided pre-loaded/ready — confirm the starter project reflects that so students don't need to clip anything themselves
:::

------------------------------------------------------------------------

## Overview

This exercise is the first hands-on block of the Week 1 GIS workshop. You will import two vector layers — a land use layer and a neighbourhood boundary layer — for the municipality of **Vlissingen**, style them, add an OpenStreetMap basemap, and produce a basemap screenshot for the Miro board.

**Learning Outcomes**

By completing this exercise, you will be able to:

✅ Import vector layers into QGIS from the Browser Panel\
✅ Check a layer's feature count and coordinate reference system (CRS)\
✅ Add an OpenStreetMap basemap using the QuickMapServices plugin\
✅ Style a categorical vector layer by attribute\
✅ Export a styled map view as an image for sharing

------------------------------------------------------------------------

## Data & Software

::: {.callout-note appearance="minimal" icon="true"}
**You will need:**

| Resource | Type | Description |
|------------------------|------------------------|------------------------|
| `[project-name].qgz` | QGIS project | Starter project for the municipality of **Vlissingen** |
| `bbg.shp` | Shapefile | Land use layer (Bestand Bodemgebruik), pre-clipped to the study area |
| `[cbs-neighbourhoods].shp` | Shapefile | CBS neighbourhood boundaries, pre-clipped to the study area |
:::

------------------------------------------------------------------------

## Step 1: Import and Explore the Data

### 1.1 Open the Project and Import the Layers

1.  Launch **QGIS LTR 3.44**
2.  Open the project `[project-name].qgz`
3.  From the **Browser Panel**, import the land use layer `bbg.shp`
4.  From the **Browser Panel**, import the neighbourhood boundary layer `[cbs-neighbourhoods].shp`

**Result:** Both the `bbg` and `[cbs-neighbourhoods]` layers should now appear in the **Layers Panel** and draw on the canvas over Vlissingen.

\[Add screenshot: QGIS window after both layers are loaded, with the Layers Panel visible\]

### 1.2 Explore the Layers

:::: {.callout-note appearance="minimal" icon="true"}
**❓ Question:** How many features does the `bbg` layer have?

**✅ Solution (click to reveal):**

::: {.callout-tip collapse="true"}
Open the **Attribute Table** (right-click the layer → **Open Attribute Table**) and check the feature count shown at the bottom-left of the table window.
:::
::::

:::: {.callout-note appearance="minimal" icon="true"}
**❓ Question:** What CRS is the project using? What CRS is the `bbg` layer in?

**✅ Solution (click to reveal):**

::: {.callout-tip collapse="true"}
Check the bottom-right corner of the QGIS window for the project CRS, and right-click the layer → **Properties → Information** for the layer's CRS. Dutch national datasets such as BBG and CBS boundaries are typically distributed in **EPSG:28992 (Amersfoort / RD New)**.

\[TA: confirm this matches the actual starter project.\]
:::
::::

------------------------------------------------------------------------

## Step 2: Add an OSM Basemap

1.  Install the **QuickMapServices (QMS)** plugin if not already installed: **Plugins → Manage and Install Plugins**, search for `QuickMapServices`, click **Install Plugin**
2.  Go to **Web → QuickMapServices → OSM → OSM Standard** to add an OpenStreetMap basemap
3.  Drag the basemap layer to the bottom of the **Layers Panel**, below `bbg` and `[cbs-neighbourhoods]`

::: {.callout-tip appearance="minimal" icon="true"}
**Tip:** To make the land use colours stand out against the basemap, right-click the basemap layer → **Properties → Symbology**, and set **Render type** to **Singleband gray**, or reduce **Brightness/Saturation**.
:::

\[Add screenshot: canvas after the OSM basemap is added, showing layer order in the Layers Panel\]

------------------------------------------------------------------------

## Step 3: Style the Layers

### 3.1 Style the Land Use Layer by Category

1.  Select the `bbg` layer, open **Layer Styling** (`F7`)
2.  Change the symbology to **Categorized**
3.  **Value:** `[bbg category field]`
4.  Choose a colour ramp and click **Classify**

::: {.callout-tip appearance="minimal" icon="true"}
**Tips:**

- Group related categories so the colour scheme reads intuitively (e.g. shades of green for vegetation, greys for built-up area)
- Decide in advance what you want the map to highlight — urbanised areas, agricultural land, or landscape features — and let that choice guide the colour scheme
:::

### 3.2 Style the Neighbourhood Boundaries

1.  Select the `[cbs-neighbourhoods]` layer, open **Layer Styling** (`F7`)
2.  Set the symbol fill to **No Fill** and choose a visible outline colour/width, so boundaries are readable on top of the land use layer

\[Add screenshot: styled map of Vlissingen with the OSM basemap, categorized BBG layer, and neighbourhood outlines, legend visible\]

------------------------------------------------------------------------

## Step 4: Export and Share

1.  Take a screenshot of the QGIS canvas that includes the legend
2.  Upload the screenshot to the course Miro board
3.  **File → Save Project**

------------------------------------------------------------------------

## Reflection

- What did you choose to highlight in your map, and how did your styling choices support that?
- How does adding the neighbourhood boundaries change how you read the land use pattern?

------------------------------------------------------------------------

::: {.callout-important title="Notes for TA"}
- Confirm `bbg` and `[cbs-neighbourhoods]` are pre-clipped to the Vlissingen study area in the distributed starter project, per the Part 1 data prerequisites in `pages/courses/mscu_q1.qmd` — students should not need to clip anything themselves in this 30-minute slot.
- Fill in the real project filename, CBS neighbourhoods filename, and the BBG category field name (currently `[bracketed]` placeholders) once finalised.
- Confirm the CRS answer in Step 1.2 against the actual starter project.
- The join/unique-values/area-in-hectares content from the original draft was intentionally cut for time — see `CLAUDE.md` if you want to reuse that structure for a later, longer part (e.g. Part 3's attribute tables + choropleth exercise).
:::
